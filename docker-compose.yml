version: '3.8'
services:
  chatbot:
    container_name: chatbot
    restart: always
    build:
      context: ./ww-js.musicbot
    volumes:
      - shared-data:/usr/src/app/media
      - /usr/bin:/usr/src/app/chrome





volumes:
  shared-data:



